spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pessoasdb?createDatabaseIfNotExist=true
    username: root
    password: ${USER_PASSWORD:root}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
